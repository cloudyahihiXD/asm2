<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <style>
        body {
            background-color: black;
        }

        ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
            background-color: #333;
        }

        li {
            float: left;
        }

            li a {
                display: block;
                color: white;
                text-align: center;
                padding: 14px 16px;
                text-decoration: none;
            }

                li a:hover {
                    background-color: #111;
                }
    </style>
</head>
<body>
    <div>   
        <ul>
            <li>@Html.ActionLink("FPT", "Home", "Trainee", new { area = "" }, new { @class = "navbar-brand" })</li>
            <li>@Html.ActionLink("Home", "Home", "Trainee")</li>
            <li>@Html.ActionLink("View list", "List", "Trainee")</li>
            <li>@Html.ActionLink("Update profile", "Update", "Trainee")</li>
        </ul>
    </div>
    <div>
        @RenderBody()
    </div>
</body>
</html>
